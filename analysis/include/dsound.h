/* Analysis-only DirectSound stub. Not used by the real build. */
#ifndef ANALYSIS_DSOUND_H
#define ANALYSIS_DSOUND_H
typedef struct IDirectSound IDirectSound;
typedef struct IDirectSoundBuffer IDirectSoundBuffer;
#endif
